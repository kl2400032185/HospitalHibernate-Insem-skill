<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- ─── Database Connection Settings ─── -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/fsadexam?useSSL=false&amp;serverTimezone=UTC&amp;allowPublicKeyRetrieval=true</property>
        <property name="hibernate.connection.username">root</property>
<property name="hibernate.connection.password">1234</property>
        <!-- ─── Hibernate Settings ─── -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!--
            hbm2ddl.auto options:
              create       - drops & recreates table each run (use for first run)
              update       - updates schema without dropping data (use after first run)
              validate     - only validates schema
              create-drop  - drops table when SessionFactory closes
        -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Show generated SQL in console (set to false in production) -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- ─── Entity Mapping ─── -->
        <mapping class="com.klef.fsad.exam.Hospital"/>

    </session-factory>
</hibernate-configuration>
